import React from 'react';

import Button from './Button';
import Input from './Input';

const Form = () => (
  <form className='search-form'>
    <Input />
    <Button />
  </form>
);

export default Form;
